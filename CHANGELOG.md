# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/), and this project adheres to [Semantic Versioning](https://semver.org/) once versioned releases begin.

## [Unreleased]

### Added
- **A case study in `/insights`** — SNF Construction Group's 10% month-over-month social reach growth (`content/insights.ts`, slug `snf-construction-group-social-reach`). It introduces **no new facts**: the client name, the figure, the "content and social management on a retainer, measured monthly since takeover" framing are all already published in `content/services.ts` (`ServiceProof` on the `design` line) and the homepage featured-work section. The piece restates that one number and explains what makes it credible — compounding vs. a spike, why the engagement is a retainer not a fixed scope (per `content/engagement.ts`), why a measurement window makes a percentage mean something — and is explicit about what it deliberately omits (no vanity baseline, no invented tactics list, no self-written quote).
  - **`InsightPost` gains an optional `kind: "note" | "case-study"`**, defaulting to a note. Only the label treatment reads it: the listing and detail pages prefix the date with "Case study · " when set. The file header's old "these are not case studies" note is rewritten to describe the stricter bar a case study is held to instead. `app/sitemap.ts` picks the new slug up automatically — it already maps over `INSIGHTS`.
- **A favicon** (`app/icon.svg`, `app/apple-icon.png`). The site had none — not a wrong one, none at all: no icon convention file in `app/`, and `/favicon.ico` returning 404 in production — so every tab showed the browser's fallback globe. Next's App Router file convention does the registration, so `layout.tsx` is untouched; the build now emits `/icon.svg` and `/apple-icon.png` as static routes and injects both `<link>` tags.
  - **It is deliberately not `public/logo.svg` renamed**, which is the obvious move and produces an invisible icon for half the audience. That file is the *on-dark* brand asset — its own header says the "J" ships white — and tab bars follow the OS theme, so a white-on-transparent mark disappears on a light one. Giving the icon its own `#141414` tile (neutral-950, taken from `tailwind.config.ts` rather than eyeballed) means it stops borrowing the browser's background and reads the same in both themes.
  - **The mark is also not square** — 285.06 × 214.44, roughly 4:3, against a favicon slot that is square everywhere it renders. It is fitted rather than stretched: scaled to 400 of 512 wide and centred, which is as large as it goes before the tile starts reading as cropped. Colours are untouched, per `docs/DESIGN.md` §2.1's rule that the neutral/blue letterform split is the mark's signature.
  - **`apple-icon.png` is rasterised from the same SVG** at 180×180 via sharp, so the two cannot drift. Apple's touch icon does not accept SVG, which is the only reason a second file exists; iOS applies its own rounded mask over the full-bleed tile.
- **Scroll-scrubbed stages — one mechanism, four sections** (`docs/MOTION_REDESIGN.md` §5.8, direction recorded as §2's third amendment). Direction given with theradiuslab.com as the reference, and the complaint behind it was precise: the tours *step*. An `IntersectionObserver` swaps which vignette is showing, so between wipes nothing moves at all — a step, a hold, a step. Scrubbing means the section moves exactly as far as the visitor moved it and stops when they stop.
  - **`lib/use-scroll-progress.ts` writes one number.** `--p`, 0 to 1 across a track, set on the pinned element; every derived value is CSS arithmetic off it. A beat's fade-in / hold / fade-out is `min()` of two ramps against sub-ranges of `--p` — the convention is documented once in the `.stage-*` block of `globals.css` rather than re-derived per section. No JS computes a transform, an opacity or a colour. **No GSAP, no Lenis, no `scroll-timeline`**: §7's "not needed, not added" still holds, and the reference site turns out to do the same thing — `position: sticky`, one custom property, `calc()`.
  - **It is a hook, not a component**, because the two tours own their DOM: their track is a two-column grid and the pinned stage is one of its children, so there is no wrapper to hand them. `components/motion/ScrollStage.tsx` is a thin shell for everything else.
  - **`--p` is a registered `@property` of `syntax: "<number>"`.** The style system stores a parsed number instead of re-tokenising a string every frame, and its `initial-value: 0` is simultaneously the server render, the pre-hydration frame, the no-JS state and the reduced-motion state. Every stage is authored so `--p: 0` is a coherent resting frame, never a blank one.
  - **A new `/services` film section** (`components/motion/ThreadFilm.tsx`): a frame that starts as a card and grows into the viewport with its corners melting from 34px to square, four beats fading through beside it on their own sub-ranges. Beat timings were tuned against a measured legibility sweep, not by eye — the first handoff left 226px of scroll with nothing readable where every other left ~110px, and the closing beat ended inside the track, leaving the last 3% an empty frame.
  - **Its resting frame is authored, not captured** (`ThreadStill.tsx`): a weave with one thread pulled, drawn on `currentColor` in the same family as the ten vignette scenes. The section is complete before the video loads and stays complete if it never does. `ScrubVideo.tsx` holds a `requestVideoFrameCallback` seek-lock so a fast scroll cannot queue seeks faster than the decoder retires them, and stays transparent until `loadeddata` rather than painting a black box over the still. **The source ladder ships empty** — no footage exists yet, and pointing at absent files would cost every visitor a failed request for no picture. Encodes must be **all-intra**; with a normal GOP a backward scrub decodes most of a keyframe group per frame and no code fixes it.
  - **The homepage hero pins for 250svh**, and its second act is the shard field closing. `lib/hero-shards.ts` holds the field in two arched bands kept off the copy by `Y_INSET`; that inset exists to protect the headline, so once the headline has ridden out it is released and the bands converge on the space it left — narrowing on X as well as Y, so the field gathers rather than flattening. `.hero-shard-mask` opens on the same schedule, since the mask and `Y_INSET` are the same guarantee twice. **The scrub only ever moves copy *out***, so `--p: 0` is the untouched hero and the LCP headline never waits on a scroll event.
    - **Corrects an earlier claim in this entry's own history.** The hero first shipped at 125svh on the reasoning that the field is an *annulus* and the centre is therefore empty by construction, making a second act impossible without new geometry. That was wrong: `hero-shards.ts:48-58` records that an annulus was the first idea and was rejected in favour of two bands. A constant can move; a ring cannot. Checking what the geometry actually is, rather than trusting a remembered description of it, is the transferable part.
  - **Measured at the §7 mid-tier baseline** (375×812, 4× CPU, 4 cores, production build, continuous fling rather than the stepper §7 warns gives a false 60fps): **both tours hold 60.0fps with zero frames over 32ms**, against 55.6fps / 66ms / 4 LoAF for the same section before this pass. The hero and the film measure 50–59fps and 43–59fps respectively — **stated as ranges on purpose**: the film, with no code change, measured 59.2fps early in a session and 42.9fps late in the same one, and an interleaved A/B of the hero swung 54.6→59.6fps within a single arm. Thermal state moves this method by ~±5fps. §7 now records how to read that, because a single run cannot separate a regression from a warm laptop. The hero's convergence is perf-neutral by that A/B: `--len` 250 measured 58.5fps mean against 56.7 for 125.

### Changed
- **Dark is now the default theme in fact, not just on paper.** `globals.css` has always held the dark tokens on bare `:root` with light as the `[data-theme="light"]` override, and its own header comment says "Dark mode is the default per DESIGN.md §4". The init script in `layout.tsx` disagreed: it consulted `prefers-color-scheme: light` and handed any light-mode OS the light theme on first paint. So the CSS, the design doc, and the script were two-to-one, and the script was winning. It now defaults to dark and only an explicit stored choice moves off it.
  - **Normalized against the literal `'light'` rather than `stored || 'dark'`.** The falsy-OR form writes whatever is in `localStorage` straight through to `data-theme`, so a junk value lands as an attribute matching no CSS block — visually dark by accident, since the dark tokens are the unqualified `:root`, but with `ThemeToggle` reading a theme that does not exist. The attribute is now always exactly one of the two valid values.
  - **`color-scheme` was never set anywhere, and this is what made it matter.** Without it the browser renders its *own* UI light — scrollbars, the `<select>` on `/contact`, autofill backgrounds, focus rings — against a dark page, and cannot paint the canvas dark before CSS arrives, which is the white flash on first load. Added as `dark` on `:root` and `light` in the override, so it tracks the theme rather than being asserted once.
  - **Verified against the case that was actually broken**, which is not the default one: a browser with `prefers-color-scheme: light` and a cleared `localStorage`. First visit renders `data-theme="dark"`, `color-scheme: dark`, `#141414` body — where before it rendered light. Also confirmed the escape hatch still works, since defaulting to dark is only correct if light remains reachable: toggling writes `light`, and it survives a reload and a cross-page navigation with the `<select>` inheriting it.
- **The site is live on `jarvisstudios.net`** (2026-08-20), closing the staged rollout in `docs/PRD.md` §9. **It was never a DNS cutover**, which is the part worth recording: `TODO.md` had carried it for two weeks as "point the domain at this Vercel deployment," and both the README and the TRD described a registrar-level DNS switch with a TTL-lowering step and a propagation wait. A lookup before touching anything showed the domain already resolving to `216.198.79.1` — Vercel's own anycast IP — behind Cloudflare nameservers, serving a *different* Vercel project. Going live was therefore a domain reassignment between two projects in one dashboard: no registrar change, no nameserver change, no Cloudflare record edit, and a rollback measured in seconds rather than a propagation window. The plan that was written down described work that did not need doing; five minutes of `nslookup` and `curl -I` against the live domain replaced it.
  - **Cloudflare is DNS-only here, not proxying.** A public resolver returned Vercel's real IP rather than a `104.x`/`172.x` Cloudflare address, which rules out the orange-cloud proxy that is the usual source of Vercel+Cloudflare redirect loops. Worth knowing before anyone "helpfully" enables it.
  - **The old project is identifiable by its headers and is retained deliberately.** It sent bare `Strict-Transport-Security: max-age=63072000`; this repo's `next.config.ts` adds `includeSubDomains; preload`, so the header alone distinguishes which project is serving. §9's rollback clause requires the old project stay deployed, minus the domain, until this site is stable through a monitoring window.
  - **The HSTS escalation was assessed rather than assumed safe.** Cutover newly commits *every* subdomain to HTTPS for two years — a directive no later config edit can retract, since it lives in browsers that have already seen it. `mail`, `webmail`, `cpanel`, `app`, `portal`, `admin`, `staging`, `blog`, `shop`, `api`, and `dev` were probed and none resolve; MX is Cloudflare Email Routing, which is SMTP and outside HSTS's reach. Safe today, and a constraint on any subdomain added later.
  - **The inherited redirect pointed the wrong way and was corrected the same day.** The domain arrived redirecting apex → `www` — the old project's convention — while `app/sitemap.ts`, `app/robots.ts`, and `metadataBase` all declare the apex canonical, so every sitemap URL and canonical tag resolved through a redirect. Fixed in the dashboard rather than in code, because the code was already right and the apex was the intended canonical. **The status code was upgraded 307 → 308 in the same pass**, which is the part that would have been easy to miss: a 307 is *temporary* and tells search engines to keep indexing the source, so inheriting it would have quietly undercut the canonical the sitemap asserts. Verified: `www` now returns `308` to `https://jarvisstudios.net/`, and the apex serves `200` directly.
  - **Preview stopped sharing production credentials, which going live turned from a latent issue into a real one.** `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `RESEND_API_KEY`, and `SLACK_WEBHOOK_URL` are now scoped Production-only; previously Preview inherited them, so a form submission on any preview URL wrote to the production `leads` table and fired real email and Slack. The README had recorded this as "not a live risk since only `master` gets pushed" — an argument that was sound when written and silently expired twice, first when PRs entered the workflow (2026-08-06) and again when the table began holding real customer inquiries (2026-08-20). A deferred risk whose justification depends on a workflow fact needs re-checking whenever the workflow changes; nothing prompts that automatically. **The intended failure mode on preview is a 500** from `/api/leads` — `getSupabaseServerClient()` throws, the route handler catches — chosen over building full isolation immediately because it stops the bleed in one dashboard toggle. Full isolation (second Supabase project, separate Resend/Slack) remains open in `TODO.md`.
  - **Verified in production, not assumed from a green deploy:** new copy served on `/`, `/about`, and `/services`; `robots.txt` and `sitemap.xml` resolving; `/api/leads` returning `405` to a GET, which is the correct answer from a POST-only handler and confirms the route deployed. No lead was submitted to test the form — that path writes to the production `leads` table and fires real email and Slack notification, so a synthetic row was not worth the liveness signal.
- **Site-wide copy repositioned from a general software agency to trades and local service businesses.** The previous copy sold capabilities ("Software that ships. Growth that compounds.", a six-service list); the new copy sells a specific, expensive, recognisable failure — a phone that rang and nobody answered. Hero, homepage case study, the `/services` diagnostic and its four situation cards, the thread-film beats, `/about`'s headline and three trust points, and the contact form's placeholders and CTA ("Send message" → "Get my free scope").
  - **The `/services` diagnostic changed what it asks.** Its four cards were phrased in product-team vocabulary — "We're starting something from scratch", "Too much of this is still manual" — which reads as a question about software. They now name trade-business symptoms (missed calls, manual booking and follow-up, not being found, an outdated site), and `content/situations.ts` remaps each to the service lines that answer it. The overlapping-mapping principle in that file's header is unchanged and still deliberate.
  - **`ServiceExplorer`'s standing instruction was deleted rather than rewritten.** Its "Pick whichever is closest… nothing is hidden" paragraph duplicated the new page subhead sitting ~200px above it, and the file's own comment already warned that two choose-one instructions in a row read as the page asking twice.
  - **`content/services.ts` leads with AI Automation and CRM.** Array order is the render order on both the homepage tour and the `/services` grid, so the two lines that answer the new hero's promise now come first instead of fourth and fifth.
  - **`/about` stopped saying "Team bios are coming soon."** That was the last coming-soon admission shipping, and the same pattern that got `/work` removed. It now argues the lean team is the point rather than apologising for a missing section.
- **The six service lines on `/services` are a comparison grid, not a stack** (`docs/MOTION_REDESIGN.md` §5.9). The DIRECTION CONTRACT was reopened and rewritten for it, and the reason is a job rather than a look: `PRODUCT.md:11` names **side-by-side comparison across services** as one of three things this page serves, and a vertical list is structurally incapable of it — a reader can only ever hold one service in view. Six cards in one composition is what comparison is.
  - **Answering the diagnostic now promotes rather than filters.** Matching services move to the front and span the full width with the vignette beside the copy instead of above it — `grid-column: 1 / -1` keyed off `data-promoted`, one DOM order, never a second branch rendered in JS.
  - **Nothing is hidden in any state, which is stricter than what it replaces.** Every card carries its name, summary, all three capabilities, its proof where one exists, its CTA and its vignette, at rest and under every filter. The old arrangement collapsed non-matching services to a compact row and needed a "Read anyway" control to undo itself; both are gone. Verified in the *served* HTML and in all four filtered states: 6 cards, 18 capabilities, 6 CTAs, 6 vignettes, every time — and `vg-armed` still absent from the server output, so JS-off keeps complete illustrations.
  - **The reorder animates via the View Transitions API, and that is not a third FLIP attempt.** The two failures this file's history records were Motion's layout system computing transforms in JS and stranding them — a `scaleY(3.3)` that never unwound, offsets to 1400px that never animated back. The browser captures and composites here, so no JS-held transform exists to strand, and where the API is absent the change simply applies, which is the instant reorder the page already shipped. Checked against that exact failure: after every transition and after toggling every situation back off, no card carries a computed transform and no `::view-transition` animation persists. Reduced motion is guarded twice — `matchMedia` before the call, and CSS zeroing the pseudo-element animations — and applies the promotion with zero running animations.
  - **`align-content: start` was the bug behind ragged CTAs**, not the CTA rule: `margin-top: auto` has no slack to push into until the body fills the card. Measured 20px and 81px of misalignment across the two rows, now 0px on both. A row of CTAs at six different heights is what stops a grid reading as a comparison.
  - **`ServiceListRail` is deleted** — "how far through the six am I" is a question a stack raises and a grid does not, when all six are on screen. Its hard-won lesson is kept as a comment in `ServiceRail.tsx` because it applies site-wide: `useScroll({ target })` cannot track a ref owned by an ancestor, and fails silently at `scaleY(0)` forever when it does.
  - **Cost:** client chunks 1,740,494 → 1,740,255 bytes, *down* 239 — deleting the rail and the `LIST`/`ITEM` variant tree more than pays for the transition code. Scroll profile across the page's other sections unchanged. `docs/DESIGN.md` §6.2 and its alternating-blocks row were stale on two counts (five services, an arrangement refused twice) and are corrected.
  - **Not built, though planned:** per-card scroll scrub. The grid changed the premise — cards pass through the viewport quickly, index-based sub-ranges don't map to position once a grid reflows across breakpoints, and six scroll drivers is real cost for a few px of drift. The existing `whileInView` arrival and armed vignette wipe already answer to scroll, and both are position-based, which is what a grid wants.
- **Both service tours now scrub, and both dropped Motion.** `useScroll` feeding a motion value into the rail's `scaleY`/`scaleX` became `transform: scaleY(var(--p))`, so `motion/react` leaves both tour bundles and each section has one progress source instead of two measuring the same element.
  - **The discrete channel stayed.** §5.7 decision 2 still holds — the `IntersectionObserver` handoff is exact by construction and the arithmetic alternative still drifts at the ends. The angular `clip-path` wipe is unchanged. What was added is continuous sub-motion within each layer's slice of the track, which is what removes the "it steps" feel without gutting a well-argued file.
  - **The drift applies to all three layer states, not just the active one.** Scoping it to `[data-state="active"]` reads tighter and introduces a snap: at a handoff the outgoing layer loses the rule while its local progress is still ~1, so it jumps 9px back to rest inside the sliver the wipe has not yet clipped away.
- **The logo mark shrinks from 36px to 28px** (`components/ui/Logo.tsx`, `h-9` → `h-7`). At 36px it stood more than twice the height of the 16px wordmark beside it and read as a badge with a caption rather than as a lockup. **`--nav-h` is unaffected and that is the thing to check**: the header's 69px comes from `py-4` (32) plus its tallest control (36), which is the CTA's `py-2`/`text-sm` on desktop and the `h-9` menu button on mobile — not the mark. Verified 69px measured against 69px declared at both breakpoints; `globals.css:95` records that a silent drift here misaligns every pinned surface on the site.
- **`isConstrainedClient()` split out of `detectHeroTier()`** (`lib/hero-capability.ts`). The film shares the reduced-motion / save-data / slow-connection / low-memory checks but not the WebGL probe: a device with no WebGL context plays H.264 perfectly well, and gating the film on `detectHeroTier() !== "static"` would have denied it to visitors who could see it fine.
- **`PRODUCT.md`'s evidence inventory is amended** to record that authored abstract motion imagery now exists and that any future footage stays abstract. Principle 1 is untouched — what it forbids is imagery presented *as evidence*, and an abstract film depicts nothing and claims nothing.
- **`.claude/launch.json` gains a production preview** (`next start -p 3210`). §7 requires motion and perf claims to be measured against a production build; the dev server's uncompiled chunks and HMR runtime make frame timings meaningless.
- **The film frame is dark in both themes**, which is not a theming oversight. The veil is a dark gradient, the beat copy is white so it survives arbitrary footage, and the still draws on `currentColor`. A themed frame gave grey hairlines on near-white under a grey vignette, plus white beat copy that vanished wherever it overhung the frame — the beats are now sized to exactly the frame's width for that last reason.
- **The ten authored scenes are redrawn as software rather than as wireframes.** Every label in `ServiceVignette.tsx`'s six service scenes and `ProcessVignette.tsx`'s four process scenes was a grey rounded bar at 13–34% opacity on an unfilled outline — which is the skeleton-loader idiom exactly, and read as six interfaces that had not finished loading rather than as pictures of the work. The subject matter, composition and one-accent-per-scene rule are unchanged; what changed is that they are now *drawn*.
  - **Three things separate a picture of software from a placeholder, and all three were missing.** Real type (nav items, column headers, stage names, tab labels, commit messages); filled surfaces at more than one elevation, so foreground and background separate; and ordinary UI furniture — search fields, avatars, tab bars, icons, a filled area under the usage line. Bars survive only where real UI genuinely shows blocks: runs of body copy, table cells, code.
  - **The bars that remain are the honest drawing, not a leftover.** Account names in `SaasVignette`'s table and `CrmVignette`'s cards stay bars deliberately: a plausible-looking customer list is exactly the fabricated evidence `PRODUCT.md:65` refuses to invent. Every word that *is* set is a generic interface noun — "Overview", "Qualified", "Review" — with no client names, no metrics, and still no axis values on either figure.
  - **New `components/services/vignette-kit.tsx`** holds the shared primitives (`Frame`, `Label`, `Surface`, `Avatar`, `Glyph`, `TextRun`, the hairlines and the elevation ladder). Both scene files previously carried their own copy of `Frame`/`hair`/`hairSoft`; the enrichment would have been written twice and drifted apart on the next touch. Server-rendered and dependency-free — neither file may gain `"use client"`, since both are passed to their tours as slots specifically to stay out of the client bundle.
  - **Elevation is a tint of `currentColor`, not a fixed panel colour.** A hard-coded surface that reads as raised on the charcoal ground is invisible on the light one; a tint lightens against dark and darkens against light, so one number is correct in both without the scene knowing which ground it landed on.
  - **The accent splits in two.** A blue *fill* — the CTA pill, the swatch, the chart area, the live dot — stays literal `#00ADEF` in both themes, because a mark is neither text nor a UI border. A blue *word* or glyph now follows `--accent`, which the light theme maps to brand-800: `docs/DESIGN.md` §2.2 records that brand-500 on white is 2.55:1 and fails even the 3:1 component minimum, and the redraw is the first version to set accent-coloured text at all.
  - **The animation contracts are untouched**, and were the constraint the redraw worked inside: the 3:2 `viewBox`, `.vg-scene` for the clip-path wipe, `.vg-accent` for the beat that lands last, and `.pv-1`/`.pv-2`/`.pv-3` for the process assembly order. No CSS changed. Verified across both themes by rasterising the *served* markup for all ten scenes rather than trusting a screenshot of one.
  - **Cost:** the homepage's served HTML grows 14.0 → 23.9KB gzip and `/services` 14.8 → 20.3KB, covering three copies of the six scenes (each renders twice by design, plus the RSC payload); client chunks grow 6.1KB raw. Inside the `docs/MOTION_REDESIGN.md` §4.2 budget, and no new JS executes — the additions are static markup.
  - **Real screenshots now have a slot rather than a rewrite.** Both files carry an empty `SCENE_IMAGES` map; adding a path paints that scene's WebP inside the same `<svg>` via `SceneImage`, so the box, aspect ratio and wipe all still come from the frame every consumer sizes against. Empty on purpose — `PRODUCT.md:58` still holds, and `public/` still contains only fonts and the logo.
- **The logo is a clean vector redraw**, closing the first open question in `docs/DESIGN.md` §10. `public/logo.svg` goes from ~250KB of auto-traced path data — roughly 240 near-duplicate anti-aliasing fragments plus a baked-in black rectangle, the artifact of running the original JPEG through a trace tool — to **three polygons on transparency, 1.4KB**.
  - **Both workarounds the old asset forced are gone.** `components/ui/Logo.tsx` no longer contains the mark in a fixed black rounded box, and no longer crops a 4:3 mark into a square with `object-cover`. Those existed only to make an opaque square survive a light background.
  - **The mark is now inlined rather than fetched, and that is what makes it themeable.** The "J" is `currentColor`, so it takes `--text-primary` and follows light/dark; the "S" stays literal `#00ADEF`. Inlining is not a preference here: an SVG referenced through `<img>` is an isolated document, so `currentColor` inside it resolves against the SVG's own root rather than the page — a themed mark is simply unreachable that way. Verified in both themes: the "J" computes `rgb(255,255,255)` on dark and `rgb(34,34,34)` on light, the "S" is `rgb(0,173,239)` in both, and the nav still measures exactly 69px so `--nav-h` stays correct.
  - **The blue is literal, not `--accent`.** `--accent` darkens to brand-800 in light mode to clear WCAG contrast for text and borders; a logo is neither, and shifting the mark's blue between themes would trade recognisability for a rule that does not apply to it.
  - The supplied file used `#00aeef`, one hex digit off the brand `#00ADEF` recorded in `PRODUCT.md`; corrected in both the standalone asset and the inlined copy. The standalone file keeps a white "J", which is the correct on-dark form for a brand asset consumed anywhere else.
  - **Not included:** only the monogram was redrawn, not the wordmark. Every current usage sets the wordmark as live Clash Display text, so nothing is blocked — but a lockup for favicons, OG images or print would still need one. Recorded in §10.

### Fixed
- **Every `/insights/[slug]` page was 404ing** — not just the new case study, all four. `app/insights/[slug]/page.tsx` typed `params` as `{ slug: string }` and read it synchronously, but Next 16 makes `params` a `Promise`; with `dynamicParams = false` the route matcher never resolved a slug and fell through to `not-found`. `generateMetadata` and the page component are now `async` and `await params`. Verified against a production build: `four-steps` and `snf-construction-group-social-reach` both return 200 and render, and an unknown slug still 404s.

### Added
- **The `/services` process section is now a scroll-scrubbed pinned timeline** (`docs/MOTION_REDESIGN.md` §5.7). It was the one part of the page §5.6 left structurally alone: four tab buttons and a crossfading paragraph, three of four descriptions behind a click, no visual at all. Four steps of copy now move past a sticky stage that hands off between four authored scenes, with a horizontal four-node timeline beneath it. `components/services/ProcessTour.tsx` + `ProcessVignette.tsx`.
  - **This is not a reversal of §5.5 decision 1.** That declined to turn the *six service lines* into a tour, because `ServiceExplorer` owns them and the DIRECTION CONTRACT refuses six alternating blocks. The process steps are different content, specified as a numbered sequence in `docs/DESIGN.md` §3.2 from the start — and named there as the one sanctioned exception to the "no decorative 01/02/03" rule, because the ordering is functionally meaningful.
  - **Four authored SVG scenes**, in the same family as the six `ServiceVignette` scenes: one 480×320 frame, `currentColor` at varying opacity, exactly one `#00ADEF` element each — three inputs converging on a marked finding, two artboards with the reviewed one outlined, a commit spine with the commit in flight, a shipped frame with a live indicator. No raster assets: `PRODUCT.md:58` records that no screenshots or photography of any kind exist and `:61` requires imagery to be authored and clearly generic. The Launch monitoring line is shape without scale for the same reason `SaasVignette`'s is — an axis invites reading a number nobody measured.
  - **Each scene assembles rather than appears** — three groups at 0 / 90 / 180ms with the accent last at 300ms, reusing `.vg-accent` rather than inventing a second vocabulary for the same idea. Keyed on the layer's state rather than on `once`, so it re-runs scrolling back up.
  - **The architecture is `ServiceTour`'s, reproduced deliberately**, including the three details in that file that exist because the obvious version was wrong: the active index comes from an `IntersectionObserver` band rather than arithmetic on `scrollYProgress`, boundary hits re-test containment against the centre line with a half-open interval instead of trusting entry order, and the observer is rebuilt on `resize`. The handoff gesture (`.tour-layer`) and the stage frame (`.tour-stage-card`) are shared outright; only ~10 pin declarations are duplicated, and both CSS blocks now point at each other.

### Removed
- **`components/ProcessSteps.tsx`** and its complete WAI-ARIA tabs implementation. Worth being precise about why: the pattern is not being abandoned as too costly to maintain — **the content no longer has a selector**. The timeline's baseline layout shows all four steps and all four visuals simultaneously, so the three hidden panels the tabs cost are gone, along with the roving tabindex and the `aria-controls` wiring that had to be kept correct. Verified after the change: zero `role="tab"`/`tablist"`/`tabpanel"` on the page, no dangling `process-panel` id, and the only remaining `aria-controls` is the nav's.

### Added
- **Motion pass on `/services`** (`docs/MOTION_REDESIGN.md` §5.6). The page had five `Reveal` fade-ups, one list re-form that only fires *after* the visitor answers, and hover colour changes; the six service lines that are the substance of the page had no scroll motion at all, and the six vignettes had none of any kind. §5.5 skipped this route deliberately and that reasoning stands — what it declined was six alternating blocks and a second pinned tour, not motion as such. The information architecture is unchanged: diagnostic first, everything expanded by default, nothing hidden, `/services#<id>` deep links intact, no pin, no parallax on text, no scroll-jacking, no FLIP on the list.
  - **The H1 arrives word by word** (`components/ui/RevealWords.tsx`), transform-only via the same `lcpSafe` reasoning as `Reveal`: Chrome excludes `opacity: 0` elements from LCP candidacy, so fading a page's headline defers its own LCP timestamp. Each word is a real text node carrying its own trailing space, so screen readers, Reader Mode, translation and copy/paste all get an ordinary sentence — the `aria-label` + `aria-hidden` alternative would hide the visible text from translation for a purely decorative split.
  - **Hairlines draw themselves** (`components/ui/Hairline.tsx`). They were already the site's primary divider, so the seam arriving costs no new furniture — a `scaleX` on the rule that was there. Used at the three section seams, between service lines, and above each engagement fact. Reduced motion needs no special case: `scaleX` is a transform, so `MotionConfig` strips it and the rule renders complete.
  - **Each service line settles as it is scrolled to, and this could not use `Reveal`.** `Reveal` animates by variant *label* and declares no `animate` prop, so nested inside `ServiceExplorer`'s `LIST`/`ITEM` tree it inherits the parent's `visible` propagation and fires on the answer instead of on scroll. The arrival layer is inline `initial`/`whileInView` objects, which a parent label cannot drive. The answer's staggered re-form is untouched and the two layers compose.
  - **The situation buttons take the §5.1 angular wipe** — the second accent moment §2's amendment 1 sanctions for interaction feedback. A stationary pre-tinted panel revealed by `clip-path`, not a translating panel with a colour transition, per §5's own note: a translating panel needs the label colour to move in lockstep and the two never quite agree.
  - **The vignettes wipe in and their single brand-blue element lands a beat later**, which is the animation restating the premise `ServiceVignette` was drawn on — every scene is greyscale except the thing the service actually changes. Reuses `.tour-layer`'s clip-path polygons rather than inventing a second "swap a visual" gesture.
  - **A scroll-linked progress rail at `lg`, a swipeable chip strip below it.** The strip is additive: the vertical list is untouched beneath it and the chips are ordinary anchors to section ids that already existed. Six duplicated service names buys a phone-sized touch target for a rail that is otherwise a 1px line in a gutter phones do not have.
  - **The four process tabs share one sliding indicator** instead of four fading underlines. This is the only `layoutId` on the page and does not contradict `ServiceExplorer`'s recorded FLIP failures: those were `layout` on tall sections whose *size* had to animate, which Motion does by scaling. A positioned 2px bar never changes size. The tabs' own arrival is on the buttons, not on wrappers — a `div` between `role="tablist"` and `role="tab"` breaks the ownership the pattern depends on, and this component exists partly to not do that.

### Fixed
- **`useScroll({ target })` silently tracked nothing, and the failure had no symptom but a rail that never moved.** The progress rail was first written as a child of the list it measures, with the ref passed down from `ServiceExplorer`. React attaches a parent's ref only after its entire subtree has committed, so the nested rail's layout effect read `target.current === null` — no warning, no error, `scaleY(0)` forever. Caught by reading the computed transform at three scroll positions and getting `matrix(1, 0, 0, 0, 0, 0)` at all three. `ServiceListRail` now renders the tracked element itself. The rule: a component may rely on refs to elements it renders, never on a ref owned by an ancestor it sits inside.
- **`whileInView` cannot observe the element it collapses.** `Hairline` first put `initial={{ scaleX: 0 }}` and `whileInView` on one node — and Motion watches the element it animates, so the observed box had zero width and the section seams never drew however far they were scrolled past (`matrix(0, 0, 0, 1, 0, 0)`, permanently). The observer now sits on the full-width track and the fill takes its state by variant propagation. The variant names are deliberately not `hidden`/`visible`: a Hairline rendered inside another variant tree — which is exactly where the service lines put it — would otherwise inherit that tree's propagated label and redraw on every state change. Verified on a normal scroll-through: 13 of 13 hairlines drawn, six service lines arrived, six vignettes entered.
- **The vignettes' hidden state is armed by JS rather than being the default**, so a JS-off, Reader Mode or failed-hydration visitor gets complete illustrations rather than six blank boxes. Verified in the prerendered HTML: zero `vg-armed`, six `vg-scene`, seven `vg-accent`. Arming is additionally skipped for any card not starting fully below the fold — paint, then hide, then reveal is a flash on first load and worse than no animation.

### Verification note
- **The process timeline was scrubbed end to end at 1280×800.** The active index advances 0 → 1 → 2 → 3 and back down monotonically with **exactly one** `[data-state="active"]` layer at every sampled position; the timeline fill runs 0 → 0.16 → 0.34 → 0.51 → 0.68 → 0.85 → 1 and the four nodes light in order; the handoff clip-path was captured mid-transition at intermediate values (54.8% → −2.6% → −23.3% → −28.2% → −29.7% → −30%), confirming it interpolates rather than snapping. At 375×812 the stage is hidden, the four panels are a snap rail with `overscroll-behavior-x: contain`, each carries its own visual at full opacity, and there is no horizontal page overflow. The homepage was re-checked as a regression control: six `.tour-layer`, zero `.ptour-layer`, fourteen accents at `opacity: 1` with no transform and no clip-path.
- **Reduced motion is now verified over CDP rather than structurally**, closing the gap flagged in the previous entry. `Emulation.setEmulatedMedia` at both viewports: stage `display: none`, list a plain block stack with snap off, panels `static` at `min-height: 0`, all four descriptions visible, every layer transition `0s` so no wipe is reachable, every scene group and accent at `opacity: 1` so nothing is hidden, `.svc-rail` and `.svc-strip-fill` gone. The no-preference control on the same run flips all of it back, which is what makes the reduce result mean something rather than describing a page that failed to load.
- **The scroll profile is reported as inconclusive, deliberately.** A 4×-CPU, 4-core, Slow-4G pass over the production build shows **zero long animation frames and zero long tasks** on `/services` and `/` at both viewports — that part is real and says the per-scroll script cost is absent. But the same harness reported a locked 60fps for the homepage tour, which `MOTION_REDESIGN.md` §7 records at 47.7fps under the same nominal conditions, so **the fps numbers are not comparable to that table** and are not being written into it. The cause is the scroll driver: stepping 120px and awaiting one `rAF` per step measures a paced scroll, so frames dropped during a fling never appear. Running the homepage as a control is what exposed this — without it the run would have read as a straight improvement.
- **The `/services` work was checked in a real compositing browser at 1280×800 and 375×812**, not just against the DOM: the rail fills 0 → 0.52 → 1 across the list; the strip's bar tracks a real horizontal swipe to full width; the wipe computes `clip-path 0.42s` and reaches the active polygon on press; `aria-live` still announces "Showing 4 of 6 service lines…"; the vignette resolves to its finished clip and `opacity: 1`; `/services#crm` lands 96px below the nav with everything settled; the process tabs keep `[-1, -1, 0, -1]` roving tabindex, move selection *and* focus on ArrowRight, and carry exactly one indicator, which travels between tab positions. `npm run lint`, `npx tsc --noEmit` and `npm run build` are all clean, and `/services` is still statically prerendered.
- **Reduced motion was verified structurally, not emulated, and that is weaker than this project's own standard.** Every new rule was read back out of `document.styleSheets` and confirmed to sit inside the correct query: the wipe's two clip states unconditional with only its transition gated (so the selected state still reads instantly under `reduce`), all `.vg-*` rules wholly inside `no-preference` (so reduced motion hides nothing), and both progress readouts `display: none` under `reduce` — they are motion values piped into `style`, not Motion *animations*, so `MotionConfig` has nothing to strip. The `Emulation.setEmulatedMedia` pass §7 established, and the 4×-CPU mid-tier profile against `next start`, have **not** been run for this section. Both are recorded as outstanding in §7.

### Fixed
- The stacked service panel's own vignette is capped at 32rem (`.tour-panel-visual`). Uncapped it spanned the full 1152px content column while the copy above it stopped at `max-w-md` — a diagram four times the width of the text it illustrates. Only reachable under reduced motion at desktop widths, since every other layout either hides that copy (the pinned stage owns the visual at `lg`) or is too narrow for it to matter. Found by looking at a screenshot, not by measurement: every assertion about this layout passed before and after.

### Verification note
- **Mid-tier device profiling is done, and `docs/MOTION_REDESIGN.md` §4.2's `useFrame` requirement passes.** §7 had recorded this as unmet since the hero shipped, correctly noting the real blocker was the absence of a target rather than the measuring. A baseline is now defined in §7 — 375×812, 4× CPU throttle, 4 cores, Slow-4G, low-end floor at 6×, measured against the **production** build because profiling `next dev` measures HMR and unminified React — along with the full results table.
  - **The hero's render loop holds a locked ~60fps with zero long animation frames and zero long tasks** at 4× *and* 6× throttle, on both the lite and full tiers. The per-frame cost §4.2 named as "the realistic way this regresses INP" simply is not there.
  - The tour's scroll is the looser figure and still comfortable: worst frame 66ms, worst LoAF 65ms, no long tasks — an order of magnitude inside INP's 200ms "good" threshold. The dropped frames are paint on a large sticky area under a 4× handicap, not script, which matches a design whose only per-scroll JS is one composited transform and an observer firing five times.
  - **Not covered, and no amount of CDP will cover it:** throttling applies to the CPU, not the GPU, so WebGL fill rate and thermal behaviour on real mobile silicon remain unmeasured. That is the axis that breaks first if the hero ever gains post-processing or more shards, and this method will not see it coming.
- **The motion work in this entry was verified against a real compositing browser**, not just the DOM. The in-editor preview pane never displayed during the session, and a non-displayed page reports `document.hidden === true`, in which Chrome fires neither `requestAnimationFrame` nor `IntersectionObserver` — so every animation read as frozen and two of them were briefly misdiagnosed. The workaround was headless Chrome driven over CDP from a throwaway script using Node 22's built-in `WebSocket` (no dependency added to the project). What that made checkable, and what it caught:
  - **The tour handoff genuinely advances**: active index 0 → 2 → 5 with exactly one active layer at each stop, rail fill 0 → 0.379 → 1, and the clip-path polygons captured mid-transition at intermediate values (`-44.7%`, `-27.4%`) rather than snapping between states.
  - **The route cross-fade fires**: rAF-polled across a client-side navigation, opacity reaches 0 and climbs back over 8 frames. An earlier fixed-delay sample said it never fired — that was dev-mode RSC latency, not a defect.
  - **The mobile menu genuinely rolls**: per-frame heights 0 → 12 → 72 → 130 → 163 → 182 → 196 → 209, twelve distinct values with the `ease-confident` curve visible in the spacing. A round-trip sample had read 209 at both ends and looked like a snap.
  - **`prefers-reduced-motion: reduce` is now actually tested**, via `Emulation.setEmulatedMedia` rather than the manual OS toggle previously assumed necessary: `matchMedia` agrees, the pinned stage is `display: none`, the rail is `block` with `scroll-snap-type: none`, panels are `static` with `min-height: 0`, all six carry their own vignette, and there is no horizontal overflow and no zero-height panel.
  - Still not covered, and still needing real hardware: the mid-tier device profiling outstanding in `docs/MOTION_REDESIGN.md` §7. Headless Chrome on a desktop says nothing about frame cost on a Moto G-class phone.

### Added
- Route cross-fade between pages (`app/template.tsx`), closing `docs/MOTION_REDESIGN.md` §5 item 4 — 180ms on opacity, page body only. A template rather than a layout because Next remounts one on every navigation and reuses the other, and it sits inside the root layout so `Nav` and `Footer` hold still; a header that fades along with the page reads as a full reload. Enter-only: App Router has no hook for a route leaving, and faking one by freezing the outgoing tree costs more than a true cross-fade is worth here.
  - **The fade is suppressed on first load, and that guard is the whole subtlety of this file.** Fading in unconditionally would defer the LCP of every page by the animation's own duration — the exact penalty `Reveal`'s `lcpSafe` variant exists to dodge, reintroduced site-wide one level up. It is gated on a module-scope flag (which survives the template's remount, unlike component state, which the remount resets) read behind a `typeof window` check — because module scope on the server is shared across requests, and without the check the second request a server process handled would bake `opacity: 0` into the HTML for everyone. Verified on `/`, `/contact`, `/about` and `/insights`: **zero `opacity: 0` before `<main>` in the prerendered HTML, and no `<h1>` starts hidden on any route.**
  - Opacity only. A translate would be stripped by `<MotionConfig reducedMotion="user">` anyway, and a page that slides on every navigation is the ambient motion §2 asks to avoid; a cross-fade is vestibular-safe and degrades to itself.
- `from?: "bottom" | "left" | "right"` on `Reveal` (`components/ui/Reveal.tsx`), backed by a precomputed variant table so Motion never re-diffs the set on a parent re-render. `bottom` is the default and reproduces the previous variants exactly — verified in the prerendered HTML, which still emits `translateY(16px)` at every pre-existing call site. Applied in exactly one place: the `/about` differentiators, with `from="left"`, because each row *is* a horizontal construction (the `01`/`02`/`03` marker, then the text) and arriving along that axis lands the number first; a vertical reveal makes the markers bob independently of the rule they sit against. The offset is 20px, under the site's 24px `px-6` gutter, so a full-width block can't slide past the viewport edge — verified no horizontal overflow on `/about`, with 265px of slack.
  - **The alternating left/right feature blocks §5 item 2 originally imagined were deliberately not built.** `/services` is the page they were meant for, and its DIRECTION CONTRACT explicitly refuses that arrangement. Direction is now available where a layout earns it, which is the useful half of the idea without overriding a considered decision.

### Fixed
- Three pages had scroll-reveal gaps against the rest of the site. **`/contact` had no `Reveal` at all** — its H1 arrived flat while every other page's assembled — and now uses `lcpSafe` like the others; the form deliberately stays outside the wrapper, since its Suspense skeleton exists to hold height against a 0.43 CLS regression and animating that boundary would reintroduce movement exactly where the skeleton is there to prevent it. The homepage insights callout and the closing CTA on `/insights/[slug]` were also unwrapped, the latter leaving the last block of an article sitting still after everything above it revealed.

### Added
- Open/close animation on the mobile nav menu (`components/Nav.tsx`), the last conspicuously unfinished interaction on the site — it previously mounted and unmounted with no transition at all (`docs/MOTION_REDESIGN.md` §5 item 3). The panel rolls down from `height: 0` and back over 220ms on `ease-confident` via `AnimatePresence`, and the hamburger and close glyphs rotate through each other at 120ms each way. A tap-triggered transition is *prompted* motion, so it sits inside §2's restraint rule rather than needing an exception the way the scroll tour did.
  - **The border and background moved to an inner element.** Left on the animating wrapper, `border-t` stays 1px tall the entire way down to `height: 0`, so the exit ends with a stray rule sitting directly under the header's own `border-b`. Verified after the change: the animating element computes `border-top-width: 0px` and its child `1px`.
  - **The menu was a disclosure with no keyboard exit.** Escape now closes it and returns focus to the toggle, so a keyboard user isn't stranded on a link that just unmounted; the toggle also gained the `aria-controls` that should always have paired with its `aria-expanded`. Verified: focus moved into the panel, Escape fired, `aria-expanded` returned to `false` and `document.activeElement` was the toggle.
  - `height` is the one animated property `<MotionConfig reducedMotion="user">` does not govern — it is neither a transform nor CSS — so it branches explicitly on `useReducedMotion()` down to a plain opacity change. This is deliberately the opposite call from the service tour, which refuses that hook: there the null-first window would move layout and cost a shift, whereas here nothing reads it until the visitor taps, which cannot happen before hydration.
  - No interaction with the service tour's hardcoded `--nav-h: 69px`, despite the header growing to 278px while open: the toggle's container is `md:hidden` and the tour only pins at `lg`, so the menu cannot be open at any width where that constant is read. Verified at 1440px — toggle container `display: none`, header 69px, stage `top: 93px`.

### Changed
- The homepage "What we do" section is now a scroll-linked pinned tour (`components/services/ServiceTour.tsx`) instead of a six-card grid. At `lg` the copy scrolls through six full-height panels while one `position: sticky` stage holds beside it and hands off between the six `ServiceVignette` scenes; below `lg` the same six panels become a horizontal `scroll-snap-type: x mandatory` rail. Those vignettes previously only rendered on `/services`, inside an expanded `ServiceExplorer` row — they are the best-looking asset in the repo and the homepage never showed them. No new dependency: this is `motion`'s `useScroll` plus plain CSS, which closes `docs/MOTION_REDESIGN.md` §7's open question about adding GSAP for exactly this case.
  - **Two scroll channels, neither of which puts scroll position into React state.** The continuous one is a motion value passed straight to `scaleY` on the progress rail, so Motion writes a composited transform and nothing re-renders. The discrete one — which vignette is showing — is a single `IntersectionObserver` with a 2px band at the pinned stage's centre, firing at most five times per pass. Deriving the index arithmetically from `scrollYProgress` was tried on paper and rejected: the stage centre sits `nav/2 + 24px` below the viewport centre and the track range is `(6 × panel) − 100svh`, so `Math.round(p × 5)` drifts up to ~0.13 index units at the ends, and correcting for it means baking panel and nav height into JS where they rot silently.
  - **Every layout branch is a CSS `@media` rule; the component has none.** Breakpoint and reduced-motion switching both happen in `globals.css`, so the server HTML and the hydrated tree are identical and the section contributes no CLS. This is deliberately *not* the null-first `useState` pattern `HeroVisual` uses for tier detection — that pattern is correct there (device capability is unknowable on the server) and would be a self-inflicted layout shift here. The cost is 12 vignettes in the DOM with six hidden, ~110 nodes; that is the price of the guarantee, not an oversight.
  - Reduced motion gets a *complete alternative layout*, not a disabled pin: `.tour-stage { display: none }` is the unconditional baseline, so six blocks stack down the page each carrying its own vignette. The same baseline covers no-JS and Reader Mode. Merely setting `position: static` would strand one visual beside four screens of copy. `lib/hero-capability.ts` is deliberately **not** reused — it answers "should we download a WebGL bundle", and its `saveData`/2GB-RAM/2-core checks would serve those visitors a different information architecture rather than less motion, while its WebGL probe would allocate a context on every homepage load for a section with no WebGL.
  - No scroll-jacking, and specifically no vertical snapping: across ~420svh it would trap slow scrollers and break Find-in-Page and PageDown. The section only reads scroll position. All copy stays in normal flow, so tab order, Find-in-Page and the no-JS render are unaffected — verified in the prerendered HTML, which contains exactly one `data-state="active"` and five `"future"`.
  - **The homepage grows by roughly four screens**, pushing the featured-work result and the final CTA that much further down. That is a product tradeoff, not a technical one, and it is the thing to revisit first if conversion moves.

### Removed
- `components/ServiceCard.tsx` and its `.service-card-wipe` / `.service-card-shard` CSS. The grid above was its only call site. Rather than lose the angular wipe from `docs/MOTION_REDESIGN.md` §5.1, the tour reuses the gesture as its vignette handoff — the same four-point polygon leaning 15% across the height on the same `ease-confident` curve — so the wipe is promoted from a hover flourish to the structural motion of the section, and the tour introduces no new motion vocabulary. The shard burst is not carried forward; it was tied to the card's 68px bottom band, which no longer exists.
- The investor/partner path, end to end. The contact form's type selector is gone (`components/ContactForm.tsx`), `LeadType` narrows from `"client" | "investor"` to `"client"` (`lib/types/lead.ts`), and validation rejects anything else — verified: `POST /api/leads` with `type: "investor"` returns 400 `VALIDATION_ERROR`. The contact page subhead no longer offers the investor route. Verified zero case-insensitive occurrences of "investor" in the rendered HTML of all eight routes in the sitemap.
  - Two follow-on fixes the removal forced. The project-type field was gated on `type === "client"`, which only ever hid it from the investor path, so the condition is dropped and the field always renders. And `ContactFormSkeleton` led with a standalone block standing in for the selector while omitting the project-type field — it now renders four labelled fields, matching the real form exactly rather than running one field short (that skeleton exists to hold form height; it was a 0.43 CLS regression before it was added).
  - **The `leads` table check constraint still permits `'investor'` and is deliberately unchanged.** Narrowing it would reject rows already stored under that type, and a constraint allowing a value nothing writes costs nothing. `supabase/migrations/0001_create_leads_table.sql` is applied history and is not edited in place.
  - `PRODUCT.md` and `README.md` described the investor path as live and are corrected. `docs/PRD.md`, `docs/TRD.md`, `docs/DESIGN.md`, and `docs/ARCHITECTURE.md` still specify it (an audience segment, a page, a lead type, a form toggle) and are now stale — left alone, since rewriting requirements documents is a separate decision from removing a feature.

### Added
- Closing CTA band on `/insights`, carrying the same offer as the homepage and `/services`: heading, body, risk-reversal line, then the button. The page previously ended on the last post excerpt, so a reader who got through three posts on process and pricing — warmer than a cold visitor — had nowhere to go. **This is the third hand-maintained copy of this band.** All three now state the same offer in three places, so the next wording change has to be made three times; it wants extracting into a component, which is deliberately not done here to avoid restyling two pages in a change scoped to a third. Note the `/insights/[slug]` pages still close with their own weaker ask ("Have a project this touches on? Get in touch →"), which also uses the "Have a project" phrasing already retired from the homepage.

### Changed
- `/insights` subhead drops the "not case studies, since we don't have any to publish yet" clause and points at the SNF Construction Group result on `/services#design` instead. The clause was the same coming-soon admission that got `/work` removed, and it had gone factually stale — a real result is published on two other pages, so the sentence contradicted them. Verified the `#design` anchor resolves: `ServiceExplorer` renders `id={service.id}` with `scroll-mt-24` on each service section, and the proof sits inside that block.

### Added
- The SNF Construction Group result on the Services page, attached to the Marketing & Design line. Carried by a new optional `proof` field on `Service` (`content/services.ts`) rather than a one-off `service.id === "design"` branch in `ServiceExplorer`, so a second result later is a content edit. The field is optional on purpose: a required one gets filled, and an invented number beside a real one devalues both. It renders between the capability list and the CTA, with no kicker and no panel — the capability list already ends in a hairline, and the result is specific enough not to need a label announcing it's a result. Collapsed service rows don't show it, since they show only name and summary. Same client and figures as the homepage featured-work section, deliberately not deduplicated into one string: the two surfaces phrase the surrounding sentence differently, and sharing a string would force one of them to read wrong. **This publishes the client name and figure on a second surface** — the sign-off already flagged for the homepage section covers the same facts, but it's now on two pages.

### Added
- Risk-reversal line under the Services page CTA button: "If discovery shows we're not the right fit, we'll tell you and point you elsewhere. You lose nothing but a phone call." Placed between the CTA body and the button, so the objection is cleared immediately before the ask. It shipped below the button first, on the theory that risk reversal belongs at the moment of clicking; that was wrong — below the button it only reaches readers who have already decided, and the ones it exists to convince have scrolled past it. It initially contradicted the homepage CTA, which promised "No sales call, no pitch deck" while this line prices the offer at a phone call; resolved by rewording the homepage rather than this line (see below), on the basis that discovery does involve a conversation. Consistent with `content/engagement.ts`'s "Discovery is free", which this restates as a promise rather than a fact.

### Changed
- Homepage CTA body says "No sales pitch" where it said "No sales call", resolving the contradiction with the Services page risk-reversal line ("You lose nothing but a phone call"). The homepage was the line that was wrong: discovery involves a conversation, and what the copy meant to rule out was being sold to, not being spoken to. Leaves a stutter — "No sales pitch, no pitch deck" uses "pitch" twice in four words — which a one-word change to the second clause would fix ("no slide deck"), but that clause wasn't in scope for this edit.
- Services page final CTA heading is now "Get a scoped project and a real price in 48 hours. Free." (was "Still not sure which of these you need?"). Body and button are unchanged. This is the same heading the homepage CTA now carries, word for word — deliberate, since it's one offer stated the same way in both places, but a visitor moving homepage → services meets it twice. It also means the 48-hour response commitment is now promised on two pages rather than one.
- Services page gains a subhead under the H1: "We'll tell you what it takes to get it live." The H1 itself is unchanged — it already read "Six ways we build. Usually you need two of them." It shipped first as "Tell us which. We'll tell you what it takes to get it live." and lost the opening sentence: `ServiceExplorer` opens ~200px below with "Where are you right now? / Pick whichever is closest…", so the page was issuing two choose-one instructions in a row and read as if it were asking the same question twice. The subhead is now outcome only and the explorer owns the pick-one language exclusively.
- Homepage section order: the Insights callout moves from between the featured-work section and the final CTA to below the final CTA, directly above the footer. It was sitting between the strongest proof on the page and the ask, which is the one place a secondary link shouldn't be; as a low-commitment path for readers who aren't ready to enquire, after the CTA is where it belongs. Markup is unchanged apart from position — it keeps its own `border-t`, which still reads correctly against the footer's.
- Homepage final CTA band. Heading is now "Get a scoped project and a real price in 48 hours. Free." (was "Have a project in mind?") and the body is "No sales pitch, no pitch deck. Tell us what you're building, and we'll tell you what it costs and how long it takes." (was "Tell us what you're building — we'll get back to you quickly."). The button label is unchanged. Both lines are longer than what they replace, so the heading gains `max-w-2xl` and the body widens from `max-w-md` to `max-w-xl`, both with `text-balance` — at `max-w-md` the new body ran to four ragged lines in a centered band. **The "48 hours" is a new response-time commitment**: the site made no such promise anywhere before this, so nothing contradicts it, but it is now a public undertaking rather than copy. "Free" is consistent with the existing "Discovery is free" fact in `content/engagement.ts`.
- Homepage hero copy. The H1 is now "Software that ships. Growth that compounds." (was "Web, app, and SaaS work built to actually ship.") and the subhead is "Web, app, SaaS, CRM, AI automation, and marketing & design." (was the longer "Web development, app development, SaaS, CRM, and marketing/design for businesses that need real software, not just a pitch deck."). The subhead still enumerates all six lines in `content/services.ts`, now in the service-name form ("marketing & design" matching the `design` entry's `Marketing & Design`) rather than the old prose form. It remains hand-maintained, so a seventh service line will need updating here as well as in the "What we do" grid below, which derives from `SERVICES`. `PRODUCT.md`'s positioning section quoted the old H1 verbatim and was updated to match.

### Added
- Homepage "Featured work" section, restored with a real named-client result (SNF Construction Group, 10% month-over-month social impression growth from content and social management) instead of the removed placeholder teaser. Links to `/contact` rather than `/work`, since `/work` still has no case studies to land on. Client sign-off to publish the name and figure should be confirmed before this ships to production.
- Insights section (`/insights`, `/insights/[slug]`) replacing the Investors page in the nav, footer, and sitemap: `content/insights.ts` holds three studio-authored process/engineering posts (statically rendered via `generateStaticParams`, `dynamicParams = false`), `lib/format-date.ts` renders publish dates deterministically (explicit locale + UTC timezone to avoid the hydration mismatch pattern already known from `lib/seeded-random.ts`). Homepage's investor/partner callout now points to Insights instead.

### Removed
- `/investors` page and every reference to it (nav, footer, sitemap, homepage callout). The audience-specific investor callout is replaced by a general Insights teaser; `/investors` now 404s. The contact form's `investor` lead-type option is untouched — that's a lead-routing concern, not the removed page.

- The Work surface entirely, while there is nothing real to show: the nav link, the footer link, `/work` from the sitemap, and the route itself (`/work` now 404s rather than serving a placeholder). Removing only the nav link would have left the page reachable from the footer and — worse — still listed in the sitemap, which actively asks search engines to index an empty placeholder. Restoring it is blocked on real case studies and client consent, not on code; the old placeholder is recoverable from git history if useful as a starting point.
- Homepage featured-work teaser. It read "See what we've built for real clients" and linked to `/work`, which is an intentional placeholder — the two case studies in `docs/PRD.md` don't exist yet and client consent/format is unresolved. A section that promises proof and delivers an empty page costs more trust than not having the section, so it's removed rather than restyled. **To restore**, real case studies and confirmed client consent are needed first, then a rebuilt `/work` page, then the teaser. Tracked as a follow-up.
  - Same root cause, still outstanding and deliberately not changed without a decision: the "Work" nav link and `/work` route still lead to that placeholder, and `/about`'s team bios are a placeholder too.

### Added
- Services page pass — commercial terms, per-service angular compositions, plus three defect fixes:
  - **"What working with us looks like"** (`content/engagement.ts`, `components/EngagementFacts.tsx`): free discovery, $1,000 entry point, fixed-scope or retainer, 4–8 week typical timeline. Sits after the service lines and immediately before the CTA, since these are the questions a reader would otherwise have to email to answer. Page went from ~265 to ~443 words. Figures are supplied business facts; the surrounding framing is copy and should be reviewed. Deliberately restrained — ambient content, per §2 as amended, only interaction feedback gets the bolder treatment.
    - The price and the timeline are worded so they can't be read as a pair. "$1,000 for 4–8 weeks" implies a ~$125–250/week rate that misrepresents the work, so each `detail` states its own scope explicitly: $1,000 is the floor for a small, well-defined brief, 4–8 weeks describes a typical project. Ordering also keeps them apart in the single-column mobile stack.
  - **Service block visuals** (`lib/service-shards.ts`, `lib/seeded-random.ts`): each service line now gets its own angular shard composition, generated deterministically from its id, using the same vocabulary as the hero so the page reads as one system. Replaces a 320×320 bordered panel containing an 80×80 lucide icon — ~94% empty box, repeated six times down the page — which read as a placeholder and was also the generic iconography `docs/DESIGN.md` §2.4 asked to avoid. Compositions are built along a per-service "grain" axis rather than scattered randomly, so the six read as siblings that differ in direction instead of six unrelated scatters. The hero's PRNG/rounding helpers were extracted to `lib/seeded-random.ts` and are now shared by both.
  - **Fixed: the page claimed "Five service lines" while rendering six.** Hand-written, and adding AI Automation silently falsified it; the meta description likewise omitted the new service. Both are now derived from `SERVICES`, so the next service line can't reintroduce the drift.
  - **Fixed: `ProcessSteps` claimed an ARIA tab pattern it didn't implement.** The buttons carried `role="tab"` and `aria-selected` but there was no `role="tabpanel"`, no `aria-controls`/`id` wiring, no roving `tabindex` and no arrow-key handling — announcing "tab" to a screen reader while exposing nothing it controls is worse than using no roles at all. Now implements the pattern in full per the WAI-ARIA APG. All tabs reference one stable panel id rather than per-step ids, since only the active panel is rendered and per-step ids would leave three of four `aria-controls` dangling at elements that never exist. Verified: all references resolve, roving tabindex moves correctly, ArrowRight/Left/Home/End move both selection and focus, and the panel's `aria-labelledby` re-points to the active tab.
  - **Fixed: the process section had no heading.** "OUR PROCESS" was a styled `<p>`, so the outline jumped from the `h1` straight to the service-name `h2`s. Now an `h2`, visually unchanged.
- **AI Automation** as a sixth service line (`content/services.ts`, plus `"ai"` in `PROJECT_TYPES`). Adding it to the homepage "What we do" grid alone would have shipped a card linking to a `/services#ai` anchor that doesn't exist, so it's wired through every consumer of `SERVICES`/`PROJECT_TYPES`: the homepage grid, the credibility ticker, the Services page block (with a `Workflow` icon — not `Sparkles`/`Bot`, which is the AI-icon cliché `docs/DESIGN.md` §5 warns against), the footer service links, and the contact form's project-type dropdown. No database migration needed — `leads.project_type` is unconstrained `text`, not a check constraint. Verified all six surfaces render, `/services#ai` resolves, and the dropdown offers the new option with server-side validation accepting it. Incidentally fixes the ragged last row in the homepage grid, which now fills evenly at every breakpoint.
- Service card hover treatment (`docs/MOTION_REDESIGN.md` §5 item 1, built to the bolder direction rather than the restrained micro-interaction the doc originally specified — §2 amended to match). An angular brand-blue wipe sweeps across the card on the `ease-confident` curve, the content inverts to near-black, and a burst of shards radiates from the bottom-right corner, echoing the hero's shard field at micro scale. No runtime JS — it's CSS state on `:hover`/`:focus-visible`, so it adds nothing to INP. The ticker (`Marquee`) is deliberately unchanged; §5 notes it's already correct and cheap as CSS-only.
  - The wipe is an animated `clip-path` polygon on a stationary panel, not a translating skewed panel. That's what makes the inversion safe: because the panel never moves, an inverted copy of the content sits inside it permanently pixel-aligned with the real content, so blue is only ever revealed together with the near-black text that belongs on it. The obvious first approach — translate a panel and transition the real text's `color` — leaves a ~250ms window each hover where light text sits on a half-arrived blue field.
  - brand-500 + neutral-950 is the pairing the primary CTA already uses (7.4:1) and holds in both themes, so the inverted copy needs no light/dark branching. The card border stays on `--accent` rather than brand-500: in light mode a brand-500 edge on white is only 2.55:1, under WCAG 1.4.11's 3:1 for identifying a component, while `--accent` (brand-800 there) keeps a 5.25:1 boundary around the blue fill.
  - The burst is a short wide band pinned to the card's bottom edge, not a square corner cluster, and that's a correctness constraint rather than a style choice: the card is `flex-col justify-between` with the arrow last, so the summary's final line always clears the bottom edge by at least `p-6 (24) + h-5 (20) + mt-6 (24) = 68px`, and more when a card stretches to match its grid row. A 64px-tall burst box therefore sits inside that gap by construction at every breakpoint and summary length. Earlier corner placements were positioned by eye and collided with copy as soon as the card width changed the wrap points. Verified across viewports by measuring each card's burst-box top against its summary's bottom (4px floor; 26.8px actual, since the shards don't fill their box).
  - The inverted duplicate renders with `div`s rather than `h3`/`p`, so the document keeps exactly one heading per card for anything reading the DOM outline rather than the accessibility tree; `aria-hidden` on its wrapper is what excludes it from the a11y tree. Verified 6 cards / 6 `h3`s on the homepage. Note that the browser tooling used here builds its tree from the DOM and ignores `aria-hidden` (confirmed by control test), so the a11y-tree exclusion rests on the attribute being correct per spec, not on a tool-verified tree.
  - Reduced motion handled in `globals.css`, since CSS transitions are invisible to `<MotionConfig reducedMotion="user">`: the wipe becomes an instant fill with no sweep, and the burst — which is nothing but motion — is removed rather than popped in. Verified the compiled `@media (prefers-reduced-motion: reduce)` block.
  - `focus-visible` mirrors `hover` throughout; verified keyboard focus produces the full treatment alongside the focus ring.
- Interactive 3D homepage hero — the assembling shard field (`docs/MOTION_REDESIGN.md` §3 Option C, signed off from the three concept options), built on React Three Fiber + Three.js per §4.1. Angular shards fly in and assemble on load, drift idly, parallax to the cursor by depth layer, and disperse as the hero scrolls away. Scroll response uses Motion's `useScroll` rather than GSAP, per §4.1's "don't ship two scroll engines for one effect" — GSAP remains unadded, since §7 says not to add it speculatively.
  - All four fallback tiers from §4.3 implemented, not an on/off switch (`lib/hero-capability.ts`): `prefers-reduced-motion`, save-data/slow connections, ≤2GB RAM, ≤2 cores, and missing WebGL all get the static tier; touch/narrow/mid-tier hardware gets a reduced scene (13 shards vs 30, no antialiasing, idle drift instead of cursor tracking); capable desktops get the full scene. The check runs *before* the dynamic import, so a device that can't use the 3D bundle never downloads it.
  - Verified the 866KB Three.js chunk is absent from the prerendered homepage HTML — it is fetched only after the capability check passes, so the LCP path is unchanged (§4.2).
  - The static fallback (`components/hero/HeroFallback.tsx`) draws the *same* shard data as SVG, so reduced-motion and no-WebGL visitors see the composition the 3D scene assembles into rather than an unrelated placeholder. It's server-rendered, so it's also what paints first for everyone while the 3D chunk resolves — the hero is never visually empty.
  - Accessibility per §6: the canvas sits in an `aria-hidden`, `pointer-events-none` layer with `tabIndex -1`, verified out of the tab order and the accessibility tree. Shards are banded above and below the copy and additionally masked across the middle of the hero (`.hero-shard-mask`), so nothing can render behind the headline at any point in the animation and `f8a918c`'s contrast work is untouched.
  - Perf measures beyond the dynamic import: capped device pixel ratio, geometries and materials shared across shards rather than allocated per instance, all per-frame work written straight to Three.js objects with no React state churn, opaque materials only (dispersal scales down instead of fading, avoiding transparency sorting), and the render loop fully parked via `IntersectionObserver` whenever the hero is off-screen.
  - Verified in-browser across both themes, desktop and mobile viewports: entrance, cursor parallax, scroll dispersal, the static fallback, no horizontal overflow, and a clean console.
  - Note: shard geometry is generated through a seeded PRNG with `Math.sin`/`Math.cos` results rounded, because those are implementation-defined in ECMAScript — Node and the browser disagreed in the last ULP, which broke hydration on the server-rendered fallback SVG.
- `docs/MOTION_REDESIGN.md` — Phase 2 motion/animation redesign spec. Audits `docs/DESIGN.md` §3's original motion system against what's actually built (Reveal, Marquee, ProcessSteps are live; GSAP, hero showcase moment, alternating feature blocks, route transitions are still unbuilt), then specifies the confirmed next step in detail: an interactive 3D/WebGL homepage hero (mouse + scroll reactive, three concept options pending sign-off), its performance/fallback architecture (dynamic import, tiered fallback for mobile/low-end/reduced-motion), and the sitewide motion work planned after it. Scoping only — no implementation yet.
- Ops alerting for `/api/leads`'s critical failure paths (`lib/notifications/slack.ts`'s `alertOpsFailure`, wired into `app/api/leads/route.ts`) — if the rate-limit check or, worse, the Supabase insert itself fails, a distinct `:rotating_light:` alert now posts to the same Slack webhook already used for lead notifications, instead of the failure only being visible in Vercel's function logs. Addresses the gap where a broken `/api/leads` would previously go unnoticed until someone noticed leads had stopped arriving. Deliberately reuses existing infra rather than adding a new third-party service (e.g. Sentry) — that's a bigger, separate call left to whoever owns the decision to add one. Verified the alert payload posts successfully against the real Slack webhook.
- Project scaffold: Next.js 16 (App Router) + TypeScript + Tailwind CSS, initialized per `docs/ARCHITECTURE.md`. Includes root layout with dark/light theme initialization (no-flash, `prefers-color-scheme`-aware), brand color/typography tokens from `docs/DESIGN.md` wired into Tailwind config and CSS custom properties, and centralized security response headers (CSP, HSTS, X-Frame-Options, etc.) in `next.config.ts` per `docs/SECURITY_AUDIT.md` finding #4.
- `.env.example` documenting all planned environment variables (Supabase, Resend, Slack, Upstash) per `docs/TRD.md` §9.
- `.claude/launch.json` dev server config for local preview.
- Global layout shell: `Nav` (desktop + mobile menu, theme toggle, "Start a project" CTA) and `Footer` (service/company links, contact email), wired into `app/layout.tsx` around every page per `docs/DESIGN.md` §7.
- `Logo` component: contains the current `logo.svg` brand asset in a fixed-size rounded box (interim fix per `docs/DESIGN.md` §2.1, since the source file isn't safely editable — see that section for why) and `ThemeToggle`, both under `components/ui/`.
- Stub pages for `/services`, `/work`, `/about`, `/investors`, `/contact` so navigation is fully clickable ahead of each page's real content.
- Contact form (`components/ContactForm.tsx`) and `POST /api/leads` Route Handler implementing the full contract from `docs/TRD.md` §5: client/investor type selector, server-side validation as the source of truth, honeypot bot-trap (silently returns a fake success rather than revealing detection), Upstash Redis-backed rate limiting (5/IP/hour), Supabase insert via the service-role-only server client, and best-effort concurrent Resend + Slack notifications that never block or fail the response once the lead is saved. Shared `lib/types/lead.ts` and `lib/validation/lead.ts` keep client and server validation in sync per `docs/ARCHITECTURE.md` §4.5. `lib/sanitize.ts` escapes all lead fields before they're interpolated into the email/Slack templates per `docs/SECURITY_AUDIT.md` finding #3.
- `supabase/migrations/0001_create_leads_table.sql` — the `leads` table DDL with RLS enabled and zero public policies, per `docs/TRD.md` §4.3.
- Verified in-browser against the real (unconfigured) backend: client-side validation errors, the honeypot's stealth 200 response, and the graceful 500 + generic retry message shown when Supabase/Upstash aren't configured — confirming the "lead insert is the only thing that can fail the request" reliability contract holds even under real failure conditions.
- Real homepage content per `docs/DESIGN.md` §6.1: staggered hero entrance, service summary grid (`content/services.ts`, `ServiceCard`), credibility marquee, featured case study teaser, deliberately lower-emphasis investor callout, and a final CTA band. `Reveal` (Motion `whileInView`) and `Marquee` (CSS keyframes, respects `prefers-reduced-motion`) added as reusable scroll-reveal primitives per `docs/DESIGN.md` §3.
- Services page (`docs/DESIGN.md` §6.2): interactive numbered process sequence (`ProcessSteps`, `content/process.ts`) and 5 alternating service blocks (`ServiceBlock`) with concrete per-service capabilities (`content/services.ts` expanded), each anchor-linked from the homepage/footer.
- About page (`docs/DESIGN.md` §6.4): mission statement and 3 "why Jarvis Studios" differentiator blocks (`content/differentiators.ts`). Team bios are intentionally left as an honest placeholder rather than fabricated — that's factual content about real people, not marketing copy, so it needs real input before it can ship.
- Investors page (`docs/DESIGN.md` §6.5): deliberately lower-key visual treatment and a distinct "Request investor info" CTA (outline style, not the primary brand-blue button) linking to `/contact?type=investor`.
- Work page: honest "coming soon" placeholder rather than fabricated case studies — the PRD specifies 2 real case studies with actual client details, which don't exist yet (tracked as an open question in `docs/TRD.md`).
- `ContactForm` now reads `?type=` from the URL to pre-select client vs. investor (wrapped in `<Suspense>` per Next.js's static-generation requirement for `useSearchParams`) — verified end-to-end that `/contact?type=investor` correctly pre-selects the investor tab.
- Verified via full production build (`next build`) that all marketing pages remain statically prerendered (`○`) and only `/api/leads` is dynamic (`ƒ`), confirming the Suspense addition didn't regress the SSG architecture from `docs/ARCHITECTURE.md` §4.2.
- 404 page (`app/not-found.tsx`), and generated `sitemap.xml`/`robots.txt` (`app/sitemap.ts`, `app/robots.ts`) per `docs/TRD.md` §8.5 and `docs/PRD.md` §6 item 7 — completes the "global elements" MVP checklist. Verified a real unmatched route returns HTTP 404 with the designed page, and both generated files serve correct content.
- Product Requirements Document (`docs/PRD.md`) — problem statement, target users, goals/non-goals, user stories, MVP scope, success metrics, phased rollout plan.
- Technical Requirements Document (`docs/TRD.md`) — tech stack, data models, API contracts, third-party integrations, performance/scalability and non-functional requirements.
- Architecture Document (`docs/ARCHITECTURE.md`) — system/data-flow diagrams, request lifecycle, folder/module structure, key architectural decisions with rationale.
- Design Document (`docs/DESIGN.md`) — brand color system, typography pairing, motion/animation system, light/dark theming, page-by-page layout spec.
- Security Audit (`docs/SECURITY_AUDIT.md`) — design-level review covering auth, input validation/sanitization, CORS, rate limiting, secrets handling, and OWASP Top 10 exposure, with findings by severity.
- `README.md` — project overview, planned tech stack, planned setup/env vars/scripts, folder structure, documentation index.
- `CHANGELOG.md` — this file.
- Self-hosted Clash Display (Medium/Semibold/Bold, WOFF2) via `next/font/local`, resolving the `docs/DESIGN.md` §2.3 heading font — previously fell back to system sans. License confirmed (Fontshare Free Font EULA: free for commercial/web use, unlimited time, no attribution required); full text at `public/fonts/clash-display/LICENSE.txt`. Verified via computed styles and network requests that the real font loads and renders, fully self-hosted rather than pulled from Fontshare's CDN at runtime.
- Dynamic Open Graph image (`app/opengraph-image.tsx`, `next/og`) generated on-brand (charcoal background, blue accent glow, wordmark) and automatically applied as `og:image` across every page — completes the "OG tags" part of the `docs/PRD.md` §6 item 7 SEO requirement. Falls back to a system sans instead of Clash Display, since `next/og`'s renderer only supports TTF/OTF and the self-hosted font files are WOFF2. Verified the generated image renders correctly and that every page's `<head>` picks it up.
- CI dependency scanning (`.github/workflows/dependency-audit.yml`, runs `npm audit --audit-level=high` on push/PR/weekly) and `.github/dependabot.yml` (weekly npm + GitHub Actions update PRs), resolving `docs/TRD.md` §10's open dependency-scanning question. Confirmed `npm audit` currently reports zero vulnerabilities.
- Vercel Web Analytics and Speed Insights (`app/layout.tsx`), addressing `docs/PRD.md` §7's note that the site launched without an analytics tool — most success metrics (bounce rate, mobile conversion, Core Web Vitals) were unmeasurable until this was added. Cookie-free, no consent banner required. Verified the site still builds and renders correctly with both mounted; the scripts only actually report data once deployed on Vercel, so no activity shows locally.
- Accessibility pass against the `docs/DESIGN.md` §8 WCAG AA target, resolving several checks the doc had flagged but never actually verified:
  - **Reduced motion was silently not working.** `Reveal`'s comment claimed Motion disabled scroll animations under `prefers-reduced-motion` automatically — it doesn't, without opting in via `MotionConfig`. Added `<MotionConfig reducedMotion="user">` around the app body in `app/layout.tsx`, so every scroll-reveal animation site-wide now actually respects the OS setting, per `docs/DESIGN.md` §3.1.
  - **Light-mode accent color failed contrast everywhere it was used as text or a border** — `--accent` was the same `#00ADEF` in both themes, which is only 2.55:1 on white (fails even the 3:1 UI-component minimum, let alone 4.5:1 for text). Hit the 404 page, the active process step, About page step numbers, and several hover states. Added `brand-800` (`#00749F`, 5.25:1 on white) and made `--accent` theme-aware in `app/globals.css`, fixing every usage from one token per `docs/DESIGN.md` §2.2's flagged contrast check.
  - **Form error text failed contrast in light mode** — `error-500` as text is only 3.91:1 on white (fails the 4.5:1 body-text minimum), the one place `docs/DESIGN.md` §2.2 called "load-bearing." Added a theme-aware `--error-text` token (darkened for light mode, lightened slightly for dark mode) and switched `ContactForm.tsx`'s validation/error messages to it.
  - Added a "skip to content" link (`app/layout.tsx`), satisfying WCAG 2.4.1 — previously keyboard/screen-reader users had to tab through the full nav on every page before reaching content.
- Ran actual Lighthouse audits (all 6 marketing pages, production build) against the Core Web Vitals/accessibility/SEO thresholds `docs/PRD.md` §7 requires — the site had Speed Insights instrumentation but no one had verified it was actually passing. Found and fixed real regressions:
  - **Contact page CLS was 0.43** (target: `<0.1`) — `Suspense fallback={null}` around `ContactForm` (needed for `useSearchParams`) rendered nothing, so the form popped in and shoved the footer down once it resolved. Replaced with a `ContactFormSkeleton` sized to match the real form, eliminating the shift (CLS now 0).
  - **Every page's LCP element (its `<h1>`) was delayed by its own entrance animation** — `Reveal` animates from `opacity: 0`, and Chrome excludes zero-opacity elements from LCP candidacy until they become visible, so the "showcase" fade-in was directly inflating the metric it's supposed to showcase against. Added an `lcpSafe` variant to `Reveal` (`components/ui/Reveal.tsx`) that animates only `y`/transform, never opacity, and applied it to the hero heading block on every page — cut the homepage's observed LCP element render delay from ~792ms to ~128ms.
  - **Heading order was broken on Services/Work/Investors/Contact** (`heading-order` a11y audit) — `Footer` (rendered on every page) used `<h3>` for its column headings, which skips a level on any page with no `<h2>` in the main content. Changed to `<h2>` (`components/Footer.tsx`); also fixed `ServiceBlock`'s per-service `<h3>` → `<h2>` on the Services page for the same reason. Accessibility score is now 100 on all 6 pages (was 98 on 4 of them).
  - **Homepage SEO was 92/100** — the investor callout's "Learn more" link had no context distinguishing it from any other "Learn more" on the web. Reworded to "Learn more about investing" (`app/page.tsx`). SEO is now 100 on all 6 pages.
  - Remaining note: LCP is still ~3.3–3.5s in Lighthouse's default mobile-simulated-throttling lab report (vs. the PRD's 2.5s target), down from ~3.9s. That gap is now mostly the simulated slow-network/4x-CPU-throttle model applied to the page's real ~434KB payload, not an outstanding code bug — worth revisiting with real Vercel field data (via the Speed Insights now installed) rather than chasing the lab number further.

### Changed
- Replaced the placeholder domain with the real production domain, `jarvisstudios.net`, in `app/sitemap.ts`, `app/robots.ts`, and `app/layout.tsx` (`metadataBase`, needed for correct absolute OG/canonical URLs).
- TRD and ARCHITECTURE updated to resolve Security Audit High/Medium findings: rate limiting locked to Upstash Redis (ruling out an in-memory limiter, which doesn't work reliably on stateless serverless functions), production/preview environment isolation required for Supabase and Resend/Slack credentials, explicit sanitization rules for content interpolated into email/Slack notifications, explicit same-origin/no-CORS policy for `/api/leads`, and centralized security response headers.
- DESIGN.md logo section updated after reviewing the actual brand asset (`logo.svg`): documented that the file is an auto-traced conversion of the source JPEG rather than clean vector art, and specified a safe interim usage pattern instead of attempting a risky hand-edit.
- DESIGN.md icon set decided as Lucide (over Phosphor), for consistency with the Next.js/Tailwind stack.

---

## Maintaining this file

- Add an entry under `[Unreleased]` for every meaningful commit or PR — grouped under **Added** / **Changed** / **Fixed** / **Removed** (omit empty groups per entry, don't list "N/A" placeholders once real entries exist).
- When a version is tagged/released, rename `[Unreleased]` to the version + date (e.g., `## [0.1.0] - 2026-08-15`) and start a fresh empty `[Unreleased]` section above it.
- Write entries for the reader of the changelog, not the commit log — describe the effect of the change, not the mechanics of how it was made.
